import styled from 'styled-components';

function Products() {
  return <ProductsContainer className="container">Products</ProductsContainer>;
}
const ProductsContainer = styled.div``;

export default Products;
